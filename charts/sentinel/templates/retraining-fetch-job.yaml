{{- if .Values.retraining.enabled }}

{{- $runId := required
    "retraining.runId is required when retraining is enabled"
    .Values.retraining.runId
-}}

apiVersion: batch/v1
kind: Job

metadata:
  name: "sentinel-retraining-fetch-{{ $runId }}"
  labels:
    app.kubernetes.io/name: sentinel-retraining-fetch
    app.kubernetes.io/component: retraining
    app.kubernetes.io/part-of: sentinel

spec:
  backoffLimit: 0

  template:
    metadata:
      labels:
        app.kubernetes.io/name: sentinel-retraining-fetch
        app.kubernetes.io/component: retraining
        app.kubernetes.io/part-of: sentinel

    spec:
      restartPolicy: Never
      automountServiceAccountToken: false

      {{- if .Values.registry.existingPullSecret }}
      imagePullSecrets:
        - name: {{ .Values.registry.existingPullSecret | quote }}
      {{- end }}

      containers:
        - name: retraining-fetch
          image: "{{ .Values.worker.image.repository }}:{{ .Values.worker.image.tag }}"
          imagePullPolicy: {{ .Values.worker.image.pullPolicy }}

          command:
            - python
            - -m
            - sentinel.retraining.fetch_data

          env:
            - name: POSTGRES_HOST
              valueFrom:
                configMapKeyRef:
                  name: sentinel-common-config
                  key: POSTGRES_HOST

            - name: POSTGRES_PORT
              valueFrom:
                configMapKeyRef:
                  name: sentinel-common-config
                  key: POSTGRES_PORT

            - name: POSTGRES_DB
              valueFrom:
                configMapKeyRef:
                  name: sentinel-common-config
                  key: POSTGRES_DB

            - name: POSTGRES_USER
              valueFrom:
                secretKeyRef:
                  name: {{ .Values.secrets.existingSecret | quote }}
                  key: POSTGRES_USER

            - name: POSTGRES_PASSWORD
              valueFrom:
                secretKeyRef:
                  name: {{ .Values.secrets.existingSecret | quote }}
                  key: POSTGRES_PASSWORD

            - name: RETRAINING_PIPELINE_NAME
              value: {{ .Values.retraining.pipelineName | quote }}

            - name: RETRAINING_MIN_NEW_ROWS
              value: {{ .Values.retraining.minimumNewRows | quote }}

            - name: RETRAINING_DISCARD_LABEL
              value: {{ .Values.retraining.discardLabel | quote }}

          resources:
            requests:
              cpu: {{ .Values.retraining.resources.requests.cpu | quote }}
              memory: {{ .Values.retraining.resources.requests.memory | quote }}
            limits:
              cpu: {{ .Values.retraining.resources.limits.cpu | quote }}
              memory: {{ .Values.retraining.resources.limits.memory | quote }}

{{- end }}
